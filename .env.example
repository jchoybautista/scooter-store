# Copy to ".env" and fill in to switch the data layer from local seed data to
# your real Supabase project. Without these, the app runs fully on seed data.
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
